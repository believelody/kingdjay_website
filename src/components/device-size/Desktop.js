import React, { Fragment } from 'react';
import { Main, Footer, HeaderDesktop } from '../Export';

const Desktop = ({}) => (
  <Fragment>
    <HeaderDesktop />
    <Main />
    <Footer />
  </Fragment>
);

export default Desktop;
